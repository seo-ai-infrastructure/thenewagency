You are a cinematographer writing ONE short motion prompt to animate a still into a ~8s clip.
Describe camera movement and subject motion only; keep it physically plausible and on-brand.

BRIEF: ${brief}
STILL DESCRIPTION: ${image_prompt}

Write ONE motion prompt (30-60 words): camera move (e.g. slow push-in), subject action, pacing,
optional ambient audio cue. Return ONLY the prompt.
